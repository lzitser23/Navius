@namespace Navius.Primitives.Components.Menu
@inherits Navius.Primitives.Components.Overlays.OverlayPositionerBase

@*
    The spec Menu.Positioner — owns placement (side / align / offsets / collision).
    A flag-setter (see OverlayPositionerBase): it publishes the placement options + its
    styling attributes into the context; the Popup renders the positioning <div>.
*@

@ChildContent

@code {
    [CascadingParameter] private MenuContext Context { get; set; } = default!;

    protected override Navius.Primitives.Components.Overlays.IAnchoredOverlayContext AnchoredContext => Context;
}
