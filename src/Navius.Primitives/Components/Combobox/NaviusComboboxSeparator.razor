@namespace Navius.Primitives.Components.Combobox

@*
    The Base UI Combobox.Separator — a role="separator" divider between groups.
*@

<div role="separator" aria-orientation="horizontal" data-navius-combobox-separator @attributes="Attributes"></div>

@code {
    [Parameter(CaptureUnmatchedValues = true)]
    public IDictionary<string, object>? Attributes { get; set; }
}
