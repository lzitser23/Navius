@namespace Navius.Primitives.Components.Combobox

@*
    The Base UI Combobox.Collection — a virtualization boundary in Base UI; here a thin
    structural passthrough that renders its ChildContent with no DOM of its own.
*@

@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
