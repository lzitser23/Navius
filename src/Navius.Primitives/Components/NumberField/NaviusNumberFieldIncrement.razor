@namespace Navius.Primitives.Components.NumberField
@inherits NumberFieldPart

@*
    Base UI NumberField.Increment — a stepper button that adds Step to the value.
    Out of the tab order (the input is the focus target); disabled at the Max bound.
*@

<button type="button"
        tabindex="-1"
        aria-label="Increase"
        disabled="@IsDisabled"
        data-navius-number-field-increment
        data-disabled="@(IsDisabled ? "" : null)"
        @onclick="OnClickAsync"
        @attributes="Attributes">
    @ChildContent
</button>

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }

    [Parameter(CaptureUnmatchedValues = true)]
    public IDictionary<string, object>? Attributes { get; set; }

    private bool IsDisabled => Context.Disabled || !Context.CanIncrement;

    private Task OnClickAsync() => Context.StepAsync(Context.Step);
}
