@namespace Navius.Primitives.Components.ContextMenu
@inherits Navius.Primitives.Components.Overlays.OverlayPositionerBase

@*
    The spec ContextMenu.Positioner — owns placement (side / align / offsets / collision).
    A flag-setter (see OverlayPositionerBase): it publishes the placement options + its
    styling attributes into the context; the Popup renders the positioning <div>. A context
    menu grows to the inline-end from the cursor point, so the default side is "right".
*@

@ChildContent

@code {
    [CascadingParameter] private ContextMenuContext Context { get; set; } = default!;

    protected override Navius.Primitives.Components.Overlays.IAnchoredOverlayContext AnchoredContext => Context;

    protected override string DefaultSide => "right";

    protected override string DefaultAlign => "start";
}
